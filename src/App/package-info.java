/**
 * Clase principal de la aplicación que contiene el método {@code main} para ejecutar el programa.
 * Esta clase es responsable de iniciar la ejecución del sistema, cargando los datos de usuarios, libros y préstamos,
 * y mostrando la interfaz gráfica de usuario (GUI) para interactuar con la biblioteca.
 * La clase se encarga de la inicialización de los componentes y de coordinar las operaciones entre las distintas
 * partes del sistema (como la gestión de libros, usuarios y préstamos) a través de la interfaz visual.
 */
package App;